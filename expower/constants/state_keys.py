# State keys
ON = 'on'
MODE = 'mode'
BRIGHTNESS = 'brightness'
TEMPERATURE = 'temperature'
COLOUR = 'colour'
SCENE = 'scene'
SCENES = 'scenes'

# Custom state keys
SPEED = 'speed'
SATURATION = 'saturation'
COLOURS = 'colours'

# List of state keys used in the state dictionary
STATE_KEYS = \
(
    ON,
    MODE,
    BRIGHTNESS,
    TEMPERATURE,
    COLOUR,
    SCENE,
    SCENES,
)
