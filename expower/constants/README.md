# python-expower: expower.constants

### Example: constants
```python
>>> import expower
>>>
>>> # View the header for constants
>>> print(expower.constants.__doc__.strip())
Imports:
    .schema
    .schema_keys
    .protocols
    .ports
    .state_keys
    .maps
    .scenes
    .themes
    .status_keys

    .schema.*
    .schema_keys.*
    .protocols.*
    .ports.*
    .state_keys.*
    .maps.*
    .scenes.*
    .themes.*
    .status_keys.*
>>>
>>> # Get some values
>>> expower.constants.scenes.WONDERFUL
'wonderful'
>>> 
```
